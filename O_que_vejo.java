/*

    -- Anotações

     Esses códigos que criei tema uma formação do tipo: classes, que representam moldes para criar
    objetos, nessas classes há métodos, que são comandos que dá para se fazer, uma ve criado o
    objeto. Entre elas tem as funções 'pradões' Get's e Set's, que servem para retornar um valor
    privado, por dentro da classe e mudar os valores privados por dentro da mesma classe.

    Observações gerais:

    --> Os valores private só podem ser acessados de dentro da classe;
    (daí a importância dos get e set para cada valor.)

    --> As classes definem um novo tipo;

    --> A instância da classe permite a criação do objeto e o acesso aos métodos dessa classe;

    --> Toda classe (salvo a do enum) precisa ter um ou mais método(s) construtor(es);

    --> A clase enum não tem diretamente haver com numeros, mas com controle de escolhas;

    --> Classes se relacionam atraves de outras classes;

    --> Classes consideradas padrão podem ter seus métodos remodulados numa otra classe;

    --> Todo conjunto de classes precisa ter um método main;

    --> Classes no mesmo pacote (ou pasta) se veem;

    --> Clases em pacotes diferentes se veem atraves da importação do pacote para a classe;

    --> Só a classe que contém o método main é executável;

    --> As classes padrões estão no import java.util.*;

    --> A documentação das classes padrês estão no java docs da Oracle;

    --> Java é uma linguagem que já teve muitos profissionais, então é um ambiente
    de codifição muito integrado. (O que dá para fazer para ajudar o profissional foi feito
    ou está sendo feito)

    --> A sobrecarga não acontece neste código porque não huve necessidade de utilizar o polimorfismo de tipos;

    --> A sobrescrita ocorre em várias classes que herdaram métodos escritos de uma forma generalista.

 */